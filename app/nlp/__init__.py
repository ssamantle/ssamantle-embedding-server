"""NLP package for language-model integrations.

This package is introduced as the future home for language-related engines
such as FastText and Kiwi. During the migration, modules here re-export the
existing implementations under ``app.providers`` to avoid breaking imports.
"""
